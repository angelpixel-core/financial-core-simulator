<%= turbo_frame_tag "demo-dataset-preview" do %>
  <%= render Admin::DemoDataset::PreviewModalComponent.new(
    state: @state,
    summary: @summary,
    sample_rows: @sample_rows,
    errors: @errors,
    file_name: @file_name
  ) %>
<% end %>
