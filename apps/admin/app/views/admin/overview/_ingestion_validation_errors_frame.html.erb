<turbo-frame id="overview-ingestion-validation-errors-panel" data-poll-target="container">
  <%= render partial: "admin/overview/ingestion_validation_errors", locals: {
    errors: errors,
    selected_source: selected_source,
    selected_field: selected_field,
    show_drilldown: show_drilldown,
    navigation_context: local_assigns.fetch(:navigation_context, {})
  } %>
</turbo-frame>
