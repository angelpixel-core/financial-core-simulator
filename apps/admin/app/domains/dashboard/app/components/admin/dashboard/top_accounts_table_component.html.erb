<% if @accounts.empty? %>
  <%= render Admin::Ui::EmptyStateComponent.new(message: "No account totals available.") %>
<% else %>
  <%= render Admin::Ui::DataTableComponent.new(
    columns: columns,
    rows: rows,
    table_class: "top-accounts-table"
  ) %>
<% end %>
