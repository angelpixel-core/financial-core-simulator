FCS_LOCALE=en

# Demo lock (PR5)
# 0 = disabled (default UX), 1 = enabled (single operator lock)
DEMO_LOCK_ENABLED=0
# TTL in seconds for lock recovery when holder disconnects
DEMO_LOCK_TTL_SECONDS=900
